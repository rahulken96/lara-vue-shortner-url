{
  "build": {
    "buildCommand": "composer install --no-dev && npm install && npm run build && php artisan config:clear && php artisan migrate --force"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}